name: app_core
publish_to: none
environment:
  sdk: ^3.4.0

dependencies:
  execution_protocol:
    path: ../execution_protocol
  recipe_domain:
    path: ../recipe_domain
  operation_registry:
    path: ../operation_registry
  executor_broker:
    path: ../executor_broker

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15

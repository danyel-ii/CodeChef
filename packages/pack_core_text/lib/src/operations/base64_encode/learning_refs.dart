import 'package:execution_protocol/execution_protocol.dart';

const LearningContentRef base64EncodeLearningRef = LearningContentRef(
  bundleId: 'core.text',
  operationId: 'core.text.base64.encode',
);
